import { accounts } from "./accounts";
import { MUTATIONS, PAYDOWN_METHODS } from "./consts";
import { chartData } from "./chartData";
import { plotlyConfig } from "./plotlyConfig";

export { accounts };
export { MUTATIONS };
export { PAYDOWN_METHODS };
export { chartData };
export { plotlyConfig };

export default {
  accounts,
  MUTATIONS,
  PAYDOWN_METHODS,
  chartData,
  plotlyConfig
};
